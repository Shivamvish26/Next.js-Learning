import { redirect } from "next/navigation";
import React from "react";
// Redirecting to another page of the website

export default function RedirectPage(){

    // redirect('/')

    return(
        <div>
            <h1>Redirecting...</h1>
        </div>
    )
}